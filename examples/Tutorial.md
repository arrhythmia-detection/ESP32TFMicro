# Basics

To understand the end process of deploying a TensorFlow (TF)
model to an ESP32 Microcontroller (MCU), we must
have a basic understanding of the underlying
concepts behind TF itself.
Hence, I decided to cover both the typical TF model training
and the deployment process and how they correlate to give the 
full picture of a typical model training and deployment pipeline.
For this, let's train a simple MLP model (both float 32 and 8-bit
integer quantized) and see how it is trained
and deployed for a typical ESP32 MCU.

## Simple MLP

For the sake of demonstration, I have utilized a randomly
generated synthetic dataset here instead of an actual one
to keep things simple and easy to understand.

### Float 32 MLP

```python
    # TBA
```
